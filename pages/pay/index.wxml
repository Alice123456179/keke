<view class="cart">
<!-- 地址 -->
  <view class="info">
    <view class="infoAddress">
      <view class="left">{{address.name}}</view>
      <view class="bottom">{{address.addressInfo}}</view>
    </view>
    <view class="right">{{address.phone}}</view> 
  </view>
  <!-- 购物车内容 -->
  <view class="cartContent">
    <view class="cartTitle">购物车</view>
    <view>
        <view class="cartList"
          wx:for="{{cart}}"
          wx:key="goods_id"
        >
          <view class="cartImg">
            <image mode="widthFix" src="{{item.goods_small_logo}}" />
          </view>
          <view class="cartInfo">
            <view class="goodsName">{{item.goods_name}}</view>      
            <view class="priceGroup">
              <view class="price">￥{{item.goods_price}}</view>
              <view class="num">
                <view class="center"> 
                x {{item.num}}
                </view>
              </view>
            </view>      
          </view>
        </view>
    </view>
  </view>

  <!-- 底部 -->
  <view class="jiesuan">
    <view class="total">
      <view class="totalPrice">合计：￥{{totalPrice}}</view>
      <view class="totalbottom">包含运费</view>
    </view>
    <view class="money"
          bindtap="handlePay"
    >支付({{totalNum}})</view>
  </view>
</view>
